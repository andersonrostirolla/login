import { isTouchDevice, isMobile } from './browser'
import { setSessionStorage, getSessionStorage } from './session'
import { messages } from './messages'

export {
  isTouchDevice,
  isMobile,
  setSessionStorage,
  getSessionStorage,
  messages
}